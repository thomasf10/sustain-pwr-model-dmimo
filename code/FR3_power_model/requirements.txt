# Power figures (Fig 2a, 2b) and tests
numpy>=1.21
matplotlib>=3.5

# Rate model (Fig 2c) lives in ../D_MIMO_rate and has its own requirements.txt.
# Its Sionna 38.901 backend is heavy (Sionna 2.x pulls in PyTorch, not
# TensorFlow) and needs Python >= 3.10; install it separately in that folder.
